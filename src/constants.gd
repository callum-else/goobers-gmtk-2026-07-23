class_name Constants

enum GoobState {
	GRABBED,
	THROWN,
	CONFUSED,
	RUN_TO_FIXED_POSITION,
	IDLE,
	WAITING,
	FREEZE
}

enum GameplayState {
	GAME_START,
	LEVEL_TRANSITION,
	LEVEL_START,
	GAME_OVER
}

enum LevelId {
	KEEP_OUT_CIRCLE
}

const GRAB_HEIGHT = 5
