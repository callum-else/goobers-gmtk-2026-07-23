class_name Constants

enum GoobState {
	GRABBED,
	THROWN,
	CONFUSED,
	RUN_TO_FIXED_POSITION,
	IDLE,
	WAITING,
	FREEZE,
	RUN,
	CLICKED
}

enum GameplayState {
	CONFIGURATION,
	GAME_START,
	LEVEL_TRANSITION,
	LEVEL_START,
	GAME_OVER
}

enum LevelId {
	KEEP_OUT_CIRCLE,
	NO_RUNNING
}

const GRAB_HEIGHT = 5
